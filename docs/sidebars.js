// @ts-check

// This runs in Node.js - Don't use client-side code here (browser APIs, JSX...)

/**
 * UpKeep product documentation sidebar.
 *
 * The sidebar is generated from the `docs` folder structure. Ordering is
 * controlled with `sidebar_position` front matter and `_category_.json` files.
 *
 * @type {import('@docusaurus/plugin-content-docs').SidebarsConfig}
 */
const sidebars = {
  docsSidebar: [{type: 'autogenerated', dirName: '.'}],
};

export default sidebars;
